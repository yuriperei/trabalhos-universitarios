@model IEnumerable<MustDo.Presentation.WebMVC.Models.TagViewModel>

@{
    ViewBag.Title = "Lista de tags";
}

<h2>Lista de tags</h2>

<ul class="nav nav-pills">
    <li role="presentation" class="active">@Html.ActionLink("Nova tag", "Create")</li>
</ul>
<br />
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            <ul class="nav nav-pills">
                <li role="presentation" class="alert-warning">@Html.ActionLink("Editar", "Edit", new { id = item.TagId })</li>
                <li role="presentation" class="alert-info">@Html.ActionLink("Detalhar", "Details", new { id = item.TagId })</li>
                <li role="presentation" class="alert-danger">@Html.ActionLink("Deletar", "Delete", new { id = item.TagId })</li>
            </ul>
        </td>
    </tr>
}

</table>
